# Pylon Resource Pack Generator

## Usage

## Maintenance

### Upgrading Minecraft Versions
- Update the `pack_format` under `input/pack.mcmeta`
- Update the `template/items/items.zip` to include all vanilla item model definitions from the new update